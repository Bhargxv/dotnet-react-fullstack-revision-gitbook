# 33. Rapid-Fire JavaScript Checklist

Before moving on to React, I should be able to explain:

### Variables & Types

-   What is the difference between `let` and `const`?
-   Does `const` make an object immutable?
-   What are `null` and `undefined`?
-   What are truthy/falsy values?

### Operators

-   How does `&&` behave?
-   How does `||` behave?
-   Why can `"10" + 5` produce `"105"`?

### Functions

-   What is a parameter vs argument?
-   What is an arrow function?
-   What is an implicit return?

### Arrays

-   What does `map()` do?
-   What does `filter()` do?
-   What does `find()` do?
-   Which operations mutate arrays?

### Objects

-   What is object mutation?
-   What is the difference between `user2 = user1` and `{ ...user1 }`?
-   What is a shallow copy?

### Destructuring

-   How do you destructure an object?
-   How do you destructure an array?

### Spread/Rest

-   What is spread?
-   What is rest?
-   What is the difference between them?

### Callbacks

-   What is a callback?
-   What is a higher-order function?

### Modules

-   Named export vs default export?
-   Why do named imports use `{ }`?

### Async JavaScript

-   What is a Promise?
-   What are the Promise states?
-   What does `resolve()` mean?
-   What does `reject()` mean?
-   What does `async` do?
-   What does `await` do?
-   Does `await` block the entire JavaScript thread?

### Fetch

-   What does `fetch()` return?
-   What is a `Response` object?
-   What does `response.json()` do?
-   What is `response.ok`?
-   Does `fetch()` automatically throw for HTTP 404/500?
-   How do you send JSON in a POST request?

### Scope & Closures

-   What is scope?
-   What is the scope chain?
-   What is a closure?
-   Why does a returned function remember variables from its outer
    function?

### References

-   What happens when two variables reference the same object?
-   What does spread do?
-   Why is spread only a shallow copy?

### `this`

-   What is `this` in a regular method call?
-   How does arrow-function `this` differ?

### Event Loop

-   Why does `setTimeout(..., 0)` run after current synchronous code?

------------------------------------------------------------------------
